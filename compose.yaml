services:
  nginx:
    image: nginx:alpine
    ports:
      - "0.0.0.0:8001:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
